<tr>
  <td><%= test.title %></td>
  <td><%= test.difficulty %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('.questions'), admin_test_questions_path(test) %></td>
  <td><%= link_to t('.delete'), admin_test_path(test), method: :delete, data: { confirm: t('.confirmation')} %></td>

</tr>
