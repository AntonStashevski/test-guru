<tr>
  <td><%= question.body %></td>
  <td><%= link_to 'Edit', edit_test_question_path(question, test_id: @test.id) %></td>
  <td><%= link_to 'Answers', edit_test_question_path(question, test_id: @test.id) %></td>
  <td><%= link_to 'Delete', test_question_path(question, test_id: @test.id), method: :delete, data: { confirm: "Are you sure?"} %></td>
</tr>