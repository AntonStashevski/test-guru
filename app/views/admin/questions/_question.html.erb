<tr>
  <td><%= question.body %></td>
  <td><%= link_to 'Edit', edit_admin_test_question_path(question, test_id: @test.id) %></td>
  <td><%= link_to 'Answers', admin_test_question_answers_path(question, test_id: @test.id) %></td>
  <td><%= link_to 'Delete', admin_test_question_path(question, test_id: @test.id), method: :delete, data: { confirm: "Are you sure?"} %></td>
</tr>