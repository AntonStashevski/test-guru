<%= form_with model: [@question.test, @question], local: true do |form| %>
  <%= render 'errors' %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </p>
  <p><%= form.submit %></p>
<% end %>